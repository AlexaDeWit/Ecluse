# Fourmolu configuration: the single source of truth for Haskell formatting.
#
# This file sets every value explicitly for two reasons. The rules stay legible
# without a trip to fourmolu's docs, and the layout cannot drift when the tool
# changes a default. The dev shell pins fourmolu itself via flake.lock, currently
# 0.19.x on the nixos-26.05 base.
#
# 0.19 reads `haddock-style: multi-line` more strictly than 0.14 did. It reflows a
# wrapped, multi-line `-- |` or `-- ^` Haddock into a `{- | … -}` block, and leaves
# a single-line `-- |` alone. That matches docs/haddock.md's "blocks for longer
# docs", so we keep `multi-line`. `single-line` instead rewrites the intentional
# `{- | -}` block comments, and churns far more.
#
# Do not hand-format Haskell. Run `make format` (writes) or `make format-check`
# (the gating check). See docs/style.md → "Formatting".

# Number of spaces per indentation step.
indentation: 4

# No hard column limit. We wrap by hand where it reads best.
column-limit: none

# Arrows in type signatures sit at the end of the line.
function-arrows: trailing

# Leading commas in multi-line lists, records, and export lists.
comma-style: leading

# Import/export lists optimised for clean diffs.
import-export-style: diff-friendly

# Half-indent `where` bindings.
indent-wheres: false

# No space before an opening record brace.
record-brace-space: false

# One blank line between top-level declarations.
newlines-between-decls: 1

haddock-style: multi-line
haddock-style-module: null

# Let fourmolu choose let-block layout.
let-style: auto
in-style: right-align

# Always parenthesise a single constraint.
single-constraint-parens: always

# ASCII syntax only.
unicode: never

# Preserve the author's blank lines where reasonable.
respectful: true

fixities: []
reexports: []
